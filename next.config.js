/** @type {import('next').NextConfig} */

const nextConfig = {
    // Enable styled-components via compiler options
    compiler: {
        styledComponents: true,
    },
}

module.exports = nextConfig
